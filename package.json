{
  "name": "documentation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "hugo server",
    "publish": "hugo -e production"
  },
  "hugo-bin": {
    "buildTags": "extended"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "hugo-bin": "^0.111.3"
  }
}
